export enum Roles {
    SUPERADMIN = 'superadmin',
    ADMIN = 'admin'
}

export enum Status {
    ACTIVE = "active",
    INACTIVE = 'inactive'
}

export enum OrderStatus {
    CONFIRMED = 'confirmed',
    PENDING = 'pending',
    CANCELLED = 'cancelled'
}

export enum UserRoles {
    CUSTOMER = 'customer',
    SAILER = 'sailer'
}